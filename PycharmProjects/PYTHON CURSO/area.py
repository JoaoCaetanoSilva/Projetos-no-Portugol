largura = float(input('Largura: '))
altura = float(input('Altura: '))

area = largura * altura
tinta = area / 2

print('A area é igual a: {}'.format(area))
print('Voce precisara de {} litros de tinta para pintar toda a area'.format(tinta))